{
  "name": "pizza-lab-pro",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check app.js && node --check domain.js && node --check workflows.js && node --check sw.js",
    "test:browser": "node tests/browser-smoke.cjs"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
